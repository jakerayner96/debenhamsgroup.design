.DS_Store
node_modules/
# capture screenshots are binaries — manifest is tracked, PNGs are not (cloud storage + manifest rule)
projects/*/capture/**/*.png

# dev Storybook capture — bulk dumps are binaries/large; index + themes + manifest are tracked
.context/inventory/storybook-capture/shots/
.context/inventory/storybook-capture/dom/
.context/inventory/storybook-capture/extract.json
.context/inventory/storybook-capture/preview.css
# our Storybook — build output + generated stories + deps
storybook/node_modules/
storybook/storybook-static/
storybook/stories/
